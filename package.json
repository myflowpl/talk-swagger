{
  "name": "talk-swagger",
  "version": "0.0.0",
  "license": "MIT",
  "angular-cli": {},
  "scripts": {
    "start": "ng serve --proxy-config proxy.conf.json",
    "swagger": "java -jar ./swagger-codegen-cli-2.2.2-SNAPSHOT.jar generate -i ./swagger-petstore.json -o src/app/api -l typescript-angular2"
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
